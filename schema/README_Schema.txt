tr_schema_mysql.sql

  The tables and relations to create the tree reconciliation database.

tr_create_db_with_go_terms.sql

  Create the tables for tree reconciliation database and load
  the Gene Ontology terms. This file ceated with MySQL dump.

tr_create_db_with_go_terms_tc.sql

  Create the tables for the tree reconciliation database and load
  the Gene Ontology terms and transitive closure values.
  
